# 操作紹介セクション（Zenn / note / X 共用素材）

生成物（`docs/note-images/` と `docs/design-gen/shots/article-media/` に同じものあり）：
- `op.gif` … 一行入力→自動で複式仕訳→仕訳帳に記録→ダッシュボード反映（429KB・約6秒ループ）
- `op-1-input.png` … 一行入力＋「借方:食費／貸方:現金」自動変換プレビュー
- `op-2-ledger.png` … 仕訳帳に新しい仕訳が記録された画面
- `op-3-dashboard.png` … ダッシュボード（純資産）

---

## ▼ 媒体別の貼り方

**Zenn**：画像（GIF含む）はエディタにドラッグ、またはリポジトリ連携なら `/images/` に置いて `![説明](/images/op.gif)`。GIFはそのままアニメ再生されます。
**note**：本文の該当位置に画像をドラッグ。GIFもアニメ再生されます。
**X**：投稿に **`op.gif` を直接添付**（Xが自動でループ動画化）。または `op-1〜3.png` を最大4枚添付。リンクは本文orリプライへ。

---

## ▼ そのまま使える本文（Markdown）

### 操作はかんたん。一行打つだけ

家計簿が続かない一番の理由は「面倒だから」。なので入力は **一行だけ** にしました。

![一行入力→自動で複式仕訳→記録→反映](op.gif)

`食費 1200 現金 / コンビニ` と打つと、その場で **借方:食費 ／ 貸方:現金** の複式仕訳に自動変換されます。簿記の知識はいりません。

**① 一行で入力（裏で複式に自動変換）**
![一行入力と自動変換プレビュー](op-1-input.png)

**② 仕訳帳にちゃんと記録される**
![仕訳帳に記録](op-2-ledger.png)

**③ 純資産・資産の全体像に反映**
![ダッシュボードに反映](op-3-dashboard.png)

> 入力の手間は「一行」。あとは複式簿記・貸借対照表・純資産まで、アプリが自動で組み立てます。

---

## ▼ X 投稿文の例（動画＝op.gif を添付）

```
家計簿、入力は「一行」だけにしました。

「食費 1200 現金」と打つと、裏で複式簿記の仕訳に自動変換 → 純資産まで自動で見える。
簿記の知識ゼロでOK。登録なしで試せます👇
（リンクはリプライ）
#個人開発 #家計簿 #NISA
```
※ 本文に op.gif を添付。リンク `https://app.kurofukubo.com/?guest&utm_source=x&utm_medium=social` はリプライに置く（外部リンクは本文に置くとリーチが落ちるため）。
